/* Stub sys/param.h for WASM */
#ifndef STUB_SYS_PARAM_H
#define STUB_SYS_PARAM_H
#define MAXPATHLEN 4096
#define MAX(a, b) (((a) > (b)) ? (a) : (b))
#define MIN(a, b) (((a) < (b)) ? (a) : (b))
#endif
