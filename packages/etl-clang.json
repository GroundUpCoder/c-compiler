{
  "name": "etl-clang",
  "version": "1.0.0",
  "summary": "ETL template-conformance battery (clang-built C++ — ETLCPP/etl 20.48.1 unit tests, T2 of the C++ port ladder)",
  "requires": "native-sibling:clang",
  "files": {
    "etl-clang": { "nativeApp": "etl-clang" },
    "etl-tests": {
      "content": "#!/bin/sh\n# Menu launcher: etl-clang is a tty app (the ETL conformance battery on\n# stdout), so run it in a terminal window and hold the window open for the\n# verdict. Self-location is a known-prefix probe (todos/0444): /opt/<name>\n# installed, /usr/opt/<name> baked; `[` is a hush builtin — zero spawns\n# (the old dirname-of-realpath command substitution cost four processes).\npkg=/opt/etl-clang\n[ -d \"$pkg\" ] || pkg=/usr/opt/etl-clang\nexec term sh -c \"$pkg/etl-clang; echo; echo '[etl battery done — press Enter to close]'; read _\"\n",
      "mode": 493
    }
  },
  "bin": { "etl-clang": "etl-clang", "etl-tests": "etl-tests" },
  "menu": [
    { "group": "Demos", "entry": "etl-tests", "cmd": "etl-tests" }
  ]
}
