== #723 mutation-sensitivity ledger (third counter-pass) ==
Protocol: apply ONE mutation to compiler.js's demand-link mechanism, run
tests/host/test_sdl_loadwav_diff.js (31 legs), record which legs go RED,
restore (cmp-verified against the pre-mutation copy). A leg that no mutation
can redden is recorded as such, not claimed as coverage.

M1  bagHas reverts to NAME matching (the round-3 disease):
      RED: "IDENTITY, not spelling: a static ref does not fire even when an
            external decl of the name EXISTS"  (1 leg)
    NOTE: the first campaign run of M1 produced ZERO reds — every earlier
    probe leg has an EMPTY external-identity set, so the ext.size fast path
    (a legitimate layer of the same semantics) masked the spelling
    comparison. The p_identity leg was ADDED in response: unreferenced
    extern decl of the spelling in TU2 (non-empty ext set, --no-fold so the
    per-TU prune cannot empty it) + rooted internal-static reference in TU1.
    This is the only leg that pins the node-identity comparison itself.

M2  demandSymbolDefined drops the static guard on definedVariables (the
    round-2 disease):
      RED: p_identity; "a static VARIABLE ... cannot suppress the demand";
           "a static shadow cannot block WITHDRAWAL either"  (3 legs)

M3  bagHas ignores variable references (the round-1 disease, function-only):
      RED: "a VARIABLE-keyed directive fires on an extern read";
           "a block-scope EXTERN declaration DOES fire"  (2 legs)

M4  declaration withdrawal disabled entirely:
      RED: "--no-fold: a no-LoadWAV SDL program still compiles clean";
           "--no-undefined: ... compiles clean"; p_identity;
           "a static shadow cannot block WITHDRAWAL either"  (4 legs)

M5  withdrawal keep-statics guard removed (the fourth-instance defense):
      RED: "withdrawal must NOT swallow the undefined-static diagnostic
            (keep-statics guard)"  (1 leg — p_m5_undef_static)
    CORRECTION (round 4): this entry originally claimed M5 reddens nothing
    and that the guard is "contractual, not behaviorally load-bearing /
    unpinnable-by-behavior". THAT CLAIM WAS WRONG. The guard IS load-bearing:
    it preserves the compiler's undefined-internal-function diagnostic. The
    discriminating probe (supplied by the round-4 review, reproduced on the
    tip before correcting this entry) is a static forward declaration of the
    keyed spelling that is NEVER DEFINED, compiled --no-fold: with the guard,
    "Link error: Undefined symbol 'probe_m5'" and no wasm; with M5 applied,
    exit 0 and a 173-byte wasm — the diagnostic silently swallowed. The
    original zero-red conclusion came from INCOMPLETE PROBE SELECTION: both
    of my probes supplied a later definition, which is exactly the input on
    which the guard's presence is unobservable. p_m5_undef_static now pins
    it; RED verified under M5, green on the tip.

M7  demandSymbolDefined replaced by `return false` (suppression dead) —
    added in the round-4 re-audit of "declared not proven" entries:
      RED: "a user DEFINITION of the keyed variable suppresses the builtin";
           "a user definition of SDL_LoadWAV suppresses the builtin"
      (2 legs — both suppression legs fail on the duplicate definition, so
      the suppression claims are pinned, not just declared)

M8  the __sdl_wave_alloc_countdown seam disarmed inside the embedded
    __SDL_wave.c (wave_alloc_fail returns false unconditionally) — added in
    the round-4 re-audit:
      RED: tests/unit/sdl_load_wav (the alloc-injection sweep's pinned
      per-codec site counts collapse to 0 and expected.stdout mismatches),
      so the failure-injection suite is pinned against a dead seam.

M6  demandSymbolWanted replaced by `return false` (the #718 'if (0)'
    vacuity analogue — the mechanism dead):
      RED: 16 legs, including the entire 92-fixture differential (the
      dumper no longer compiles), every positive control, every firing
      probe, and both static-shadow suppression legs. The suite is not
      vacuous wholesale.

Legs deliberately NOT mutation-broken (declared, per the honesty rule;
re-audited in round 4 after the M5 correction — the remaining two have
principled coverage stories, not incomplete probe selection):
  - the byte-witness legs' BYTE half rides #722's dead-literal prune, whose
    sensitivity is pinned by test_sdl_deadstrip's own positive controls
    (referenced/global-pinned/address-taken literals survive) — a bounded
    delegation, not an unproven claim; their ADMISSION half is covered by
    the probe legs (M1/M6).
  - the comparator red controls are self-testing by construction: they ARE
    mutations of the expectations, run on every invocation.
  (varsupp/user-definition suppression and the alloc-injection sweep were in
  this list originally; they are now PINNED — M7 and M8 above.)
