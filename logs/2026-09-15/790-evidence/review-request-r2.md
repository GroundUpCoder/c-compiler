RE-REVIEW REQUEST — #790 counter-pass. Exact new range: 7799eb32..229f8418 (branch author/790-frame-identity, pushed; the counter-pass is the single commit d5a04892..229f8418 — review that delta plus anything it touches). Author responses to your round-1 findings:

1 (BLOCK) FIXED — os/compositor.js: shmBindFor is now makeShmUploader (top-level, Node-exported). It try-locks FIRST (bounded SHM_TRY_SPIN CAS, never a wait); on contention the PREVIOUS uploaded texture stays bound whatever its generation/size (drawn into the current rect — the contract's "temporary scaling is presentation policy"), takeRetry() forces a re-submit next rAF; a new-generation entry replaces the old only AFTER its first successful upload and the old texture is destroyed only then; only a surface that never uploaded anything gets a fresh empty texture on contention. Pinned by NEW tests/host/test_compositor_shm.js (fake device recording createTexture/writeTexture/destroy; 18 checks) with a RED CONTROL reproducing the d5a04892 algorithm inline (binds a never-uploaded texture). tests/run.js now maps os/compositor.js to the host suite too (rules union; test_diff_rules still green).
2 (major) FIXED — logs/2026-09-15/790-frame-identity.md exists (design, before/after, tests, this review record; identities are in the browser evidence.json).
3 (minor) FIXED — SURFACE_CONFIGURE: ESTALE for any serial that is not a still-valid issued one (unknown/retired/committed/nothing pending), checked BEFORE the SAB shape; EINVAL only for malformed request/SAB; every ESTALE (decline included) counted. Legs updated two-sided (test_wm_frames B/C/F, test_wm.js "nothing pending").
4 (minor) FIXED — _wmFrame rejects by SERIAL only; size is not an identity (contract stated in WM.md); os-gpubox.mjs asserts framesRejected === 0 (a run of it is queued behind the heavy lock; test_wm_frames H updated: another-size frame for the committed serial is accepted).
5 (minor) FIXED — tests/lib/suite-runner.js exports CC_UNDER_LOAD=<N> to members when --under-load runs; os-ui-frames records evidence.underLoad.
6 (minor) FIXED — SH_PMISS (header word 8): host bumps it on a flip-lock miss; kernel exposes flipMisses per surface (wmList/GET_STATE) + shmFlipMisses() sum in compositor-stats; os-ui-frames asserts the delta is 0 per driver; host test asserts the header word.
7 (nit) FIXED — an undeliverable superseded re-issue (EAGAIN) retires the outstanding set, clears pending and emits EV_CONFIGURE_DECLINED for the lost target (WM.md states it); test_wm_frames leg floods the ring and pins it.
8 (nit) FIXED — no _bumpWm on decline.
9 (nit) FIXED — beginConfigure returns true on the pre-0019 early return.
10 (nit) noted in the dev log for the shipper.

Verified by the author at 229f8418: test_compositor_shm 18/18, test_surface_configure 26/26, test_diff_rules green, test_gpu_present_clamp/binding green, test_wm_frames 73/73, test_wm 171, test_wm_policy 247, test_wm_anchored 73, test_shm_ownership 6/6, test_wm_owned 67, test_wm_lifecycle 44; browser os-ui-frames.mjs PASS at 229f8418 (both drivers). Please re-review the exact tip 229f8418 with the same VERDICT/FINDINGS/COMMANDS RUN format, post it as a #790 ticket comment, then end your turn. Do not run the kernel suite or browser sweep (heavy lock).
