INDEPENDENT IMPLEMENTATION REVIEW — c-compiler ticket #790 "Shared UI configure identities and bounded CPU/GPU frame ownership" (project 019d77d8-f894-7d09-9099-4e747aa20bfb).

You are the INDEPENDENT REVIEWER, not the author. Model disclosure: you are a claude-fable-5-1 thread because the Codex reviewer lane is capped until 2026-09-19 21:30; the author (coordinator thread 01a0a417-6825-79e9-abd3-bcd59dbbd86d, also Fable 5.1) is a DIFFERENT thread with different context. Review the DIFF, never the author's self-report.

EXACT RANGE: base 7799eb32 (main) .. tip d5a04892, branch author/790-frame-identity (pushed to origin). Review from the main checkout: `cd ~/git/c-compiler && git fetch -q && git diff 7799eb32 d5a04892 --stat` then the full diff. For hands-on probing make your OWN scratch worktree: `git worktree add ~/git/c-compiler-review-790 d5a04892 --detach` and symlink node_modules: `ln -s ~/git/c-compiler/node_modules ~/git/c-compiler-review-790/node_modules; ln -s ~/git/c-compiler/tests/browser/node_modules ~/git/c-compiler-review-790/tests/browser/node_modules`. Do NOT edit ~/git/c-compiler-790 (the author's worktree) or main. Do not run the kernel suite or browser sweep (they take the machine-wide heavy lock the author's gate holds); LIGHT tests and host tests run directly with node: `node tests/kernel/test_wm_frames.js`, `node tests/kernel/test_shm_ownership.js`, `node tests/kernel/test_wm.js`, `node tests/kernel/test_wm_policy.js`, `node tests/kernel/test_wm_anchored.js`, `node tests/host/test_surface_configure.js`, `node tests/host/test_gpu_present_clamp.js`, `node tests/host/test_sdl_api_index.js`. Throwaway probe scripts of your own against kernel.js/host.js are encouraged (the fake-worker plumbing in tests/kernel/test_wm_frames.js shows how to drive the kernel RPCs).

CONTEXT TO READ FIRST: `cc-meta ticket show 790 --project 019d77d8-f894-7d09-9099-4e747aa20bfb` (body + the author's PLAN comment), ~/git/small/GUCOS-UI-CONTRACT.md section "Resize, scaling and frame ownership" (the governing contract), todos/PRINCIPLES.md (contract-anchored correctness, honest shape), logs/2026-09-15/794-owned-windows.md (the previous phase's review record, for the bar expected), CLAUDE.md "kernel.js" section.

WHAT THE CHANGE CLAIMS (verify each against the diff, do not take it on faith):
1. kernel.js: per-surface monotonic configure serials (WINDOW_RESIZED ring word [4]; SURFACE_RESIZE reply carries it), a bounded set of still-valid issued configures (WM_CFG_OUTSTANDING=4, oldest retire) plus committedSerial; SURFACE_CONFIGURE must name its serial and the SAB's SH_GEN (header word 6) must equal it; accept = still-valid issued AND newer than committed AND same dims (superseded ack re-issues the pending target under ITS serial); otherwise ESTALE with no geometry change; `decline:true` (no SAB) retires the serial + everything older, clears pending, emits WMP EV_CONFIGURE_DECLINED 0x97 {sid, serial, w, h}; EV_CONFIGURED gains a 4th word (serial). _wmFrame rejects gpu bitmaps whose serial < committed or whose size != committed buffer (closed, counted). Pointer records get word [6] = committed serial (geometry epoch) in _wmEventTo. GET_STATE/wmList expose buffer/dst/configure identities. SH_LOCK (header word 7): wmScreenshot (the ONE kernel-side front-buffer read) holds it across its copy with a bounded CAS spin; misses counted.
2. host.js: WMSH_GEN/WMSH_LOCK + layout guard; allocFb(w,h,gen); beginConfigure(win,w,h,serial) declines on allocation failure; ackConfigure names the serial, ESTALE keeps the old buffer; wmShmFlip() = flip under SH_LOCK with a bounded Atomics.wait (all three shm present sites: shmPresent, software renderer present, Dawn readback tail); gpu shipFrame passes win.serial; frameStats() probes.
3. os/compositor.js: shmBindFor keys its texture cache on (gen, seq) and try-locks SH_LOCK around the copy — never waits; contention keeps the last frame and forces a re-submit next rAF (uploadRetry); stats.shmContended.
4. compiler.js SDL veneer: SDL_GetWindowSizeInPixels / SDL_GetWindowPixelDensity / SDL_GetWindowDisplayScale with 1:1 contracts; os/doc/sdl-api-index.md regenerated.
5. os/wm_proto.h: EV_CONFIGURE_DECLINED 0x97, EV_CONFIGURED serial doc; os/image.json 294 -> 295; os/kernel-worker.js compositor-stats probes.
6. Tests: NEW tests/kernel/test_wm_frames.js (LIGHT, 69 checks), tests/kernel/test_shm_ownership.js (worker_threads producer vs kernel read, red control that reproduces tearing unlocked), tests/host/test_surface_configure.js, tests/browser/os-ui-frames.mjs + ui-frames.c (installed image, software + WebGPU, storms + frame drag, exact pixel counts; author ran it 3/3 under load x10). test_wm.js / test_wm_policy.js / test_wm_anchored.js updated for serials (two-sided edits). Docs: todos/WM.md, todos/KERNEL.md.

REVIEW FOR, at minimum: (a) correctness of the serial state machine — find an interleaving (superseded acks, declines of older serials, retirement at the bound, destroy mid-flight, process exit, ring-full EAGAIN rollback in _wmIssueConfigure) where geometry moves backward, pending state leaks, or the compositor could observe SAB dims != surface dims; (b) the SH_LOCK protocol — is the producer really never overwriting a buffer the consumer copies? Consider the compositor's re-read of SEQ under the lock, the unlocked fallback after a spin miss (is it safe/honest?), Atomics.wait on a browser main thread, the kernel thread spinning up to WM_SHM_LOCK_SPIN, and whether the sw renderer / Dawn tail / shmPresent all flip under the lock; (c) the wm-sabs handshake — can a pending SAB from a declined configure leak into a later RPC?; (d) legacy compatibility — old baked binaries (pre-#790 wasm) through the new host.js: they never see serials, is anything worse for them? A pre-#790 host against this kernel is not supported (CD26 layout guard) — confirm the guard trips loud; (e) honest shape: are the new SDL names' contracts really SDL3's? is anything SDL-named approximated? (f) test honesty: do the tests pin what the log claims (a check that cannot fail is a finding), is the red control real, does the browser test settle on pixels/markers rather than naps, are evidence claims (exact pixel counts, zero misses) actually asserted; (g) anything the ticket's Acceptance lists that is NOT covered: "resize storms and equal-size configurations; stale ack/retirement; slow compositor and fast producer; allocation failure; in-flight destruction; CPU and GPU graphical evidence with image identities"; (h) docs/comments that now lie.

OUTPUT (mandatory sections, in this order): VERDICT: APPROVE | REJECT (blocking) — with the exact reviewed range. FINDINGS: numbered, each with severity (BLOCK / major / minor / nit), file:line at the tip, the concrete failure scenario or the exact claim that is unsupported, and what would satisfy you. COMMANDS RUN: every command you executed with its verdict/exit code (tests you ran, probes you wrote — paste probe source inline if short). Post the whole review as a comment on the ticket: `cc-meta ticket comment 790 --project 019d77d8-f894-7d09-9099-4e747aa20bfb "<text>"` (or --body @file if supported) AND write it to ~/git/c-compiler-790/../790-review-round1.md is NOT allowed (author worktree) — write it to /tmp/790-review-round1.md and say so. Then end your turn. The author will counter-pass and ask you to re-review the exact new tip. Never edit product code yourself.
