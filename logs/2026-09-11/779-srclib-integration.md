# #779 distributed Foundation source requirements

The final five-ticket mapped gate found a real integration defect: the source-library drift checker compared the complete Foundation lib.json against NSObjCRuntime.h alone, although NSString, NSException, NSArray and NSEnumerator declare their implementations in their own public headers. The twelve Win32 ports still linked; the drift check and package-building callers failed.

Commit f0a36ae005e22f6d69c9294d8e5dbe551e6e5e68 adds explicit multi-header enrollment to the existing checker and unions the actual declarations. Existing single-header entries, missing/stray checks, required-header reads, namespace ownership and shipped-file checks remain. Header declarations and the include graph are unchanged. NSObject.m already includes Foundation.h, so distributed declarations do not promise a minimal transitive implementation closure. An initial test that assumed that closure was rejected and its six failures retained.

The original mapped gate at7050d4ab remains RED: kernel182/206 and browser70/74, with28 failures containing the Foundation drift diagnostic. Other mapped rows passed; Python903 passed with112 skips and a clean skip-baseline check, including an exempt native fuzz timeout. Original audit1d7e9fd8 preserves the qualified evidence; per-browser native exits were not captured by those original canonical instruments. This was a mapped gate, not the omitted netsurf-patch/full deployment tier.

After independent repair/source review, one separately granted targeted run executed the repaired checker plus all28 previously blocked members. Actual fresh results: kernel24passed/1failed, browser4passed; the checker logged61 passing assertions. The181kernel and70browser carried rows are historical evidence, not fresh executions. Audit f0843e2f preserves the complete targeted RED attempt and native dispatcher/pane exit1. All four Chromium processes recorded native exit0 and temporary-directory cleanup. Server native exits were not recorded; post-run absence is only a point-in-time observation.

The remaining test_cmdalt_e2e.js failure concerns four shadow-diagnostic assertions after package preparation succeeds. It is coordinated through existing #785; matching earlier symptoms and current source support the diagnosis, but this run did not measure the inode pair. No failure is waived. Both RED attempts and prior accepted focused evidence remain retained. Independent review accepted this exact srclib repair and affected validation for separate batch retention; final combined source/evidence acceptance and landing remain pending #785. No duplicate successful corpus, Firefox-six, broad gate, or deployment run was performed.
